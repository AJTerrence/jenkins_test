{
  "name": "jenkins_test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "NODE_ENV=production node app.js",
    "dev": "NODE_ENV=development ./node_modules/.bin/nodemon app.js",
    "prd": "NODE_ENV=production pm2 start app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "koa": "^2.2.0",
    "koa-bodyparser": "^3.2.0",
    "koa-router": "^7.1.1",
    "koa2-cors": "^2.0.3"
  },
  "devDependencies": {
    "chalk": "^2.1.0",
    "debug": "^2.6.8",
    "nodemon": "^1.8.1"
  }
}
